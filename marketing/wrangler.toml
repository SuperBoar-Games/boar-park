name = "boar-park-marketing"
compatibility_date = "2025-03-21"

# Cloudflare Pages configuration
pages_build_output_dir = "."

# No build command needed - static files only
[env.production]
# The domain will be configured in Cloudflare dashboard
# Route pattern will be: shop.superboar.com

# Optional: Add environment variables if needed later
# [env.production.vars]
# API_BASE_URL = "https://api.superboar.com"